<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Hyperlink?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<StackPane styleClass="background" stylesheets="@../Styles/material-theme.css" xmlns="http://javafx.com/javafx/25" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.example.proyectoprogra.controllers.Visualizacion.LoginController">

    <VBox alignment="CENTER" spacing="30" maxWidth="450">
        <padding>
            <Insets top="40" bottom="40" left="40" right="40" />
        </padding>

        <!-- Icono -->
        <Label text="🎂" style="-fx-font-size: 80px;" />

        <!-- Título -->
        <VBox alignment="CENTER" spacing="8">
            <Label text="SWEET HARMONY" styleClass="headline4, text-primary" />
            <Label text="Iniciar Sesión" styleClass="subtitle1, text-secondary" />
        </VBox>

        <!-- Card de login -->
        <VBox styleClass="material-card, elevation-4" spacing="20" alignment="CENTER">
            <padding>
                <Insets top="30" bottom="30" left="30" right="30" />
            </padding>

            <!-- Email -->
            <VBox spacing="8" alignment="CENTER_LEFT">
                <Label text="Correo electrónico" styleClass="subtitle2, text-primary" />
                <TextField fx:id="campoUsuario" 
                          promptText="nombre@ejemplo.com"
                          styleClass="material-input"
                          prefWidth="350" />
            </VBox>

            <!-- Password -->
            <VBox spacing="8" alignment="CENTER_LEFT">
                <Label text="Contraseña" styleClass="subtitle2, text-primary" />
                <PasswordField fx:id="campoContrasena" 
                              promptText="Tu contraseña"
                              styleClass="material-input"
                              prefWidth="350" />
            </VBox>

            <!-- Mensaje de error -->
            <Label fx:id="etiquetaMensaje" styleClass="caption" textFill="red" wrapText="true" />

            <!-- Botón Entrar -->
            <Button fx:id="btnEntrar" 
                   text="INICIAR SESIÓN" 
                   onAction="#iniciarSesion"
                   styleClass="material-button-primary"
                   prefWidth="350" prefHeight="45" />

            <!-- Link olvidaste contraseña -->
            <Hyperlink fx:id="forgotLink" 
                      text="¿Olvidaste tu contraseña?" 
                      onAction="#abrirRecuperacionDePin"
                      styleClass="material-link" />
        </VBox>

        <!-- Footer -->
        <VBox alignment="CENTER" spacing="10">
            <Label text="¿No tienes cuenta?" styleClass="caption, text-secondary" />
            <Hyperlink fx:id="openRegisterLink" 
                      text="Crear cuenta" 
                      onAction="#abrirRegistro"
                      styleClass="material-link" />
            
            <Hyperlink text="Volver al inicio" 
                      onAction="#CerrarSesionLogin"
                      styleClass="caption" />
        </VBox>
    </VBox>

</StackPane>
